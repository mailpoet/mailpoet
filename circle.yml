## Customize dependencies
dependencies:
  pre:
    - composer install
    - ./do install

## tests override
test:
  override:
    - mkdir $CIRCLE_TEST_REPORTS/mocha
    - ./do t:j $CIRCLE_TEST_REPORTS/mocha/junit.xml
