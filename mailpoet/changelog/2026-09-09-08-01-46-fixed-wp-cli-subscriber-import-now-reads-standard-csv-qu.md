# Type: Fixed

# Description

WP-CLI subscriber import now reads standard CSV quoting, so a MailPoet export imports back unchanged; files that escape quotes with a backslash are read differently than before
